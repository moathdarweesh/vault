<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Health Connect: opens our privacy-policy screen when the user taps
                 the privacy link inside the permission dialog (Android 14+). -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>

        </activity>

        <!-- Health Connect rationale screen (Android 13 and below). -->
        <activity
            android:name=".PermissionsRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Health Connect rationale screen (Android 14+). -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Voice food logging: the WebView's getUserMedia needs mic access.
         Capacitor's WebChromeClient maps the web audio-capture request to this
         runtime permission and grants it, so declaring it here is enough. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Barcode scanning + photo food logging: the WebView's getUserMedia video
         request is mapped by Capacitor's WebChromeClient to this runtime camera
         permission. `required=false` so the app still installs on camera-less
         devices (they just fall back to manual barcode entry). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Supplement + water reminders (@capacitor/local-notifications).
         POST_NOTIFICATIONS is mandatory from Android 13 (targetSdk is 36) and is
         requested at runtime the first time reminders are switched on.
         RECEIVE_BOOT_COMPLETED is what makes reminders survive a restart —
         Android drops every scheduled alarm on reboot, and without this the
         plugin's boot receiver never runs, so reminders would silently stop
         until the app was next opened.
         SCHEDULE_EXACT_ALARM asks for on-the-minute delivery, but from Android
         14 it is DENIED BY DEFAULT and the user has to find the "Alarms &
         reminders" toggle themselves. Without it the plugin re-arms each daily
         repeat with a plain non-wakeup alarm, which Doze can defer a long way —
         so a reminder that worked on day one quietly stops being on time.

         USE_EXACT_ALARM is granted at install with no toggle to find, which is
         the whole fix. It carries ONE constraint: Google Play restricts it to
         alarm-clock and calendar apps. THE VAULT is distributed by sideload from
         the repo (version.json -> raw.githubusercontent.com) and has never been
         on Play, so the restriction does not apply. IF THIS APP IS EVER
         PUBLISHED TO PLAY, THIS LINE MUST BE REMOVED — the in-app "allow exact
         alarms" row in Settings -> Reminders already covers the fallback. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Health Connect read permissions -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_VO2_MAX" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_POWER" />
    <uses-permission android:name="android.permission.health.READ_SPEED" />

    <!-- Needed so the app can detect / open the Health Connect app -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>
</manifest>
