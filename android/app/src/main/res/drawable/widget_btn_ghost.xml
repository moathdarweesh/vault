<?xml version="1.0" encoding="utf-8"?>
<!--
  A secondary action: the surface-2 token with the interactive border. In this app a
  BORDER means interactive (identity device 1), which is why a ghost button
  keeps its edge where a plain surface would not.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#17120d" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="#38FFE8D4" />
</shape>
